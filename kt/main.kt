import task.*

fun main() {
    var pathPrefix = "../data/kt-gov2"
    BeginTask().solve(pathPrefix)
    // IntegerTask().solve(pathPrefix)
    // BooleanTask().solve(pathPrefix)
    // IfTask().solve(pathPrefix)
    // CaseTask().solve(pathPrefix)
    // ForTask().solve(pathPrefix)
    // WhileTask().solve(pathPrefix)
    // SeriesTask().solve(pathPrefix)
    // ProcTask().solve(pathPrefix)
    // MinmaxTask().solve(pathPrefix)
    // ArrayTask().solve(pathPrefix)
    // MatrixTask().solve(pathPrefix)
    // StringTask().solve(pathPrefix)
    // FileTask().solve(pathPrefix)
    // TextTask().solve(pathPrefix)
    // ParamTask().solve(pathPrefix)
    // RecurTask().solve(pathPrefix)
    // DynamicTask().solve(pathPrefix)
    // TreeTask().solve(pathPrefix)
}