#include <iostream>
using namespace std;

int main()
{
	//Task("Boolean13");
	int A, B, C;
	cin >> A >> B >> C;
	bool positive = (A > 0) || (B > 0) || (C > 0);
	cout << positive;
	
	return 0;
}
