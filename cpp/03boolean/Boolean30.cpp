#include <iostream>
using namespace std;

int main()
{
	//Task("Boolean30");
	int a, b, c;
	cin >> a >> b >> c;
	bool equalSides = (a == b) && (b == c);
	cout << equalSides;
	
	return 0;
}
