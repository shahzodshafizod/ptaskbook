#include <iostream>
#include <cmath>
using namespace std;

int main()
{
	//Task("Begin12");
	float a, b;
	cin >> a >> b;
	float c = sqrt(a*a + b*b);
	float P = a + b + c;
	cout << c << P;
	
	return 0;
}
