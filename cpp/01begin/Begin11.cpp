#include <iostream>
#include <cmath>
using namespace std;

int main()
{
	//Task("Begin11");
	float a, b;
	cin >> a >> b;
	a = abs(a);
	b = abs(b);
	float jamm = a+b;
	float tarh = a-b;
	float zarb = a*b;
	float taqs = a/b;
	cout << jamm << tarh << zarb << taqs;
	
	return 0;
}
